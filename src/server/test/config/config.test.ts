/*
// mathOperations.test.js
const mathOps = require('./mathOperations');

describe('Math Operations Tests', () => {
    // Test for addition
    test('adds 1 + 2 to equal 3', () => {
        // Arrange
        const a = 1;
        const b = 2;
        const expected = 3;

        // Act
        const result = mathOps.add(a, b);

        // Assert
        expect(result).toBe(expected);

        expect(result)
    });
});*/
